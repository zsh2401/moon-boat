import { RequestHandler } from "express";
import SendRequestBody from "../../../common/data/SendRequestBody"
import SendResponseBody from "../../../common/data/SendResponseBody"
const f: RequestHandler = (req, res) => {
    const body: SendRequestBody;
    res.send();
}
export default f;